/*Tienes disponibles los fuentes y dos applets funcionado de dos relojes digitales,
 uno usando javax.swing.Timer y el otro java.util.Timer.
*/

// 1 - Uso de clase timer para repetir una accion

Timer timer = new Timer (tiempoEnMilisegundos, new ActionListener ()
{
    public void actionPerformed(ActionEvent e)
    {
        // Aquí el código que queramos ejecutar.
     }
});
...

timer.start();



// 2 - Clase en la que está el código a ejecutar
     TimerTask timerTask = new TimerTask()
     {
         public void run() 
         {
             // Aquí el código que queremos ejecutar.
         }
     };
     ....

      // Aquí se pone en marcha el timer cada segundo.
     Timer timer = new Timer();
     // Dentro de 0 milisegundos avísame cada 1000 milisegundos
     timer.scheduleAtFixedRate(timerTask, 0, 1000);